# Test tags. Golden tests are excluded from the default CI run because
# rasterization differs slightly across host platforms (macOS vs Linux).
# Run them explicitly: flutter test --tags golden
tags:
  golden:
  contract:
  a11y:
  responsive:
  security:
  release:
  integration:
  slow:
